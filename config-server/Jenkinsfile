properties([
    parameters([
        string(name: 'VERSION', defaultValue: '', description: 'Version Service')
    ])
])

pipeline {
    agent { label 'takapedia-java' }

    environment {
        IMAGE_NAME = "harbor.local:7777/takapedia/config-server"
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Validate Parameters') {
            steps {
                script {
                    if (!params.VERSION?.trim()) {
                        error "VERSION parameter is required"
                    }
                }
            }
        }

        stage('Build JAR') {
            steps {
                dir('config-server') {
                    sh 'mvn clean package -DskipTests'
                }
            }
        }

        stage('Build Docker Image') {
            steps {
                dir('config-server') {
                    sh "docker build -t $IMAGE_NAME:${params.VERSION} ."
                }
            }
        }

        stage('Push to Harbor') {
            steps {
                withCredentials([usernamePassword(credentialsId: 'harbor-creds', usernameVariable: 'HARBOR_USER', passwordVariable: 'HARBOR_PASS')]) {
                    sh """
                        echo "$HARBOR_PASS" | docker login harbor.local:7777 -u "$HARBOR_USER" --password-stdin
                        docker push $IMAGE_NAME:${params.VERSION}
                    """
                }
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                sh """
                cat <<EOF | kubectl apply -f -
apiVersion: apps/v1
kind: Deployment
metadata:
  name: config-server
  namespace: takapedia
spec:
  replicas: 1
  selector:
    matchLabels:
      app: config-server
  template:
    metadata:
      labels:
        app: config-server
    spec:
      containers:
        - name: config-server
          image: $IMAGE_NAME:${params.VERSION}
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "dev"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: config-server
  namespace: takapedia
spec:
  selector:
    app: config-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
EOF
                """
            }
        }
    }
}
